[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "embe-procurement"
version = "0.1.0"
description = "Human-approved replenishment recommendations for EmBe"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
